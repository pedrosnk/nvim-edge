# Neovim Dot files

This repo is my experimentation with neovim configuration. For future replacement on my current
neovim flow.

## Usage

To use this with your current neovim configuration. clone and symlink the cloned path to
`~/.config/nvim`

```fish
ln -s {your_path} ~/.config/nvim
```
